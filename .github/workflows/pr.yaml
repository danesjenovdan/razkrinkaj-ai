name: Automatic PRs
on:
  push:
    branches:
      - "manipulacija"
jobs:
  pull_requests:
    name: Automatic PRs
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Pull request to manipulacija-k8s
        uses: repo-sync/pull-request@v2
        with:
          destination_branch: "manipulacija-k8s"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          pr_title: "Pulling ${{ github.ref }} into manipulacija-k8s"
